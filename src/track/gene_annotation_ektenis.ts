import { SingleTrack } from 'gosling.js/dist/src/core/gosling.schema';
import { TrackMode } from './index';

export default function gene_annotation_ektenis(
    sampleId: string,
    width: number,
): SingleTrack {
    return {}
}
